The "z_ad_gene.mat" file contains z_score values.
The way I got histogram from z_ad_gene is (Matlab):
histogram(z_ad_gene, 8, 'Normalization','pdf');

The "dist_xy_{color}.mat" file contains the data of red/blue/green curve.
The first row is x, the second row is y. 